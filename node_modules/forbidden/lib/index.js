"use strict";

module.exports = function (method) {
  var forbidden = function forbidden(req, res, next) {
    if (req.method.toLowerCase() === method.toLowerCase()) {
      res.sendStatus(403);
    } else {
      next();
    }
  };

  return forbidden;
};